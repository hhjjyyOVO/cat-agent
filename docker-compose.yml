services:
  cat-agent:
    build: .
    image: ${IMAGE_REPOSITORY:-cat-agent}:v1.0.0
    container_name: cat-agent
    env_file:
      - .env
    ports:
      - "${HOST_PORT:-8010}:8010"
    volumes:
      - ./app.py:/app/app.py
      - ./core:/app/core
      - ./web:/app/web
      - ./data:/app/data
    restart: unless-stopped
